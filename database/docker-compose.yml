version: "3"
services:
  db:
    container_name: db
    image: postgres:latest
    restart: always
    env_file:
      - .env.local
    ports:
      - "5432:5432"
    volumes:
      - ./init:/docker-entrypoint-initdb.d/
