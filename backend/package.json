{
  "private": true,
  "scripts": {
    "prepare": "nitropack prepare",
    "dev": "nitropack dev",
    "build": "nitropack build",
    "preview": "node .output/server/index.mjs",
    "update-db-schema": "npx kysely-codegen --camel-case --env-file=.env --out-file=./utils/kysely/tables.g.ts --dialect=postgres"
  },
  "dependencies": {
    "kysely": "^0.26.3",
    "nitropack": "latest",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.10.7",
    "kysely-codegen": "^0.11.0"
  }
}