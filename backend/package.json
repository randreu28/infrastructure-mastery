{
  "private": true,
  "scripts": {
    "prepare": "nitropack prepare",
    "dev": "nitropack dev",
    "build": "nitropack build",
    "preview": "node .output/server/index.mjs",
    "update-db-schema": "npx kysely-codegen --camel-case --env-file=.env.local --out-file=./kysely/tables.g.ts --dialect=postgres"
  },
  "dependencies": {
    "consola": "^3.2.3",
    "kysely": "^0.26.3",
    "nitropack": "latest",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.10.7",
    "kysely-codegen": "^0.11.0"
  }
}